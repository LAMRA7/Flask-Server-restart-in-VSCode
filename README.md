# Flask-Server-restart-in-VSCode
in VSCode If you stop the server (by pressing Ctrl+C in the terminal), you’ll need to repeat these steps to start it again.  to get that http://127.0.0.1:5000/.  need code  place code on terminal  python new_app.py 
